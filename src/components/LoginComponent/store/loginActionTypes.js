const postfix = '_LOGIN';

export const loginActionNames = {
    SEND_CREDENTIALS: `SEND_CREDENTIALS${postfix}`,
    SEND_CREDENTIALS_SUCCESS: `SEND_CREDENTIALS_SUCCESS${postfix}`,
    SEND_CREDENTIALS_FAILED: `SEND_CREDENTIALS_FAILED${postfix}`
};